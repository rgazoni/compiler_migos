#include <unordered_map>
#include <string>

enum class Type {
    Int,
    Bool,
    Arit_unary,
    Logical_unary,
    Arit_binary,
    Logical_binary,
    Relational,
    Multiplicative,
    Not,
    And,
    Or,
    Unknown
};
