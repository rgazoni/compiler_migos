#ifndef generate_h
#define generate_h

#include <fstream>
#include <iostream>
#include <string>

using namespace std;

void generate(int label = -1, string instruction = "", int attribute1 = -1, int attribute2 = -1);

#endif