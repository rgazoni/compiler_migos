#ifndef generate_h
#define generate_h

#include <fstream>
#include <iostream>
#include <string>

using namespace std;

void generate(string label, string instruction = "", string attribute1 = "", string attribute2 = "");

#endif